/**
 * Example: benchmark-model.ts wired for the planes demo app.
 *
 * This is what the developer (or agent) fills in after running
 * `moose add benchmark`. Copy this to benchmark-model.ts to use it.
 */

import { aircraftStatsModel } from "planes-moose";

export const benchmarkModel = aircraftStatsModel;
