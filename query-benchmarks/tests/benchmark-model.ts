/**
 * Benchmark Model
 *
 * Wire your query model here. All benchmark tests import the model
 * through this file — change it once, all tests follow.
 *
 * TODO: Replace the placeholder import below with your actual query model.
 *
 * Example:
 *
 *   import { myQueryModel } from "my-moose-package";
 *   export const benchmarkModel = myQueryModel;
 *
 * If you don't have a defineQueryModel yet, create one in your Moose
 * app's app/queries/ directory, then import it here.
 */

// TODO: Replace with your actual query model import
// import { myQueryModel } from "my-moose-package";
// export const benchmarkModel = myQueryModel;

export const benchmarkModel = null as any; // placeholder — replace me
