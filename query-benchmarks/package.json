{
  "name": "query-benchmarks",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "bench": "dotenv -e .env.remote -- vitest run tests/query-benchmarks.test.ts"
  },
  "dependencies": {
    "@514labs/moose-lib": "^0.6.450",
    "planes-moose": "workspace:*"
  },
  "devDependencies": {
    "dotenv-cli": "^11.0.0",
    "tsx": "^4.21.0",
    "vitest": "^3.2.4"
  }
}
